export class EvidenceStore {
  async createUpload() { throw new Error('EvidenceStore adapter is not configured'); }
  async getArtifact() { throw new Error('EvidenceStore adapter is not configured'); }
}
